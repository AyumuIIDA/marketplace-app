version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  # SearchByText と SimilarItems は同形の結果(Hit[])を返すため SearchResponse を意図的に共用する。
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
